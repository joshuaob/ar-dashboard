import Route from '@ember/routing/route';
// import RSVP from 'rsvp';

// kpis -> /offers/:offer-id/kpis (for a particular offer)
// kpis -> /offers/kpis (for all offers)
//
// trends -> /offers/:offer-id/trends (for a particular offer)
// trends -> /offers/trends (for all offers)

export default class ApplicationRoute extends Route {
}
